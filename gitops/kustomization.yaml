apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ArgoCD 가 이 디렉토리를 통째로 동기화한다는 가정의 GitOps 루트.
# (로컬에서는 up.sh 가 `kubectl apply -k gitops/` 로 동일하게 적용)
resources:
  - 00-namespaces.yaml
  - apps
  - monitoring
  - loadgen
